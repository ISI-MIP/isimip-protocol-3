type: object
required:
- specifiers
properties:
  specifiers:
    type: object
    required:
    - geo_dataset
    - resolution
    - region
    properties:
      climate_forcing:
        type: string
      geo_dataset:
        type: string
      resolution:
        type: string
      region:
        type: string
