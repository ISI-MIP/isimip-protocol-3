{
  "path": [
    "(?P<simulation_round>[A-Za-z0-9]+)",
    "(?P<product>[A-Za-z]+)",
    "(?P<category>[a-z0-9-_]+)",
    "(?P<subcategory>[a-z0-9-_]+)",
    "(bias-adjusted)",
    "(?P<region>(global))",
    "(?P<timestep>[a-z0-9-]+)",
    "(?P<climate_scenario>[a-z0-9]+)",
    "(?P<climate_forcing>[A-Za-z0-9-]+)"
  ],
  "dataset": [
    "(?P<climate_forcing>[a-z0-9-]+)",
    "(?P<ensemble_member>[a-z0-9]+)",
    "(?P<bias_adjustment>[a-z0-9-]+)",
    "(?P<climate_scenario>[a-z0-9]+)",
    "(?P<climate_variable>[a-z]+)",
    "(?P<region>(global))",
    "(?P<timestep>[a-z0-9-]+)"
  ],
  "file": [
    "(?P<climate_forcing>[a-z0-9-]+)",
    "(?P<ensemble_member>[a-z0-9]+)",
    "(?P<bias_adjustment>[a-z0-9-]+)",
    "(?P<climate_scenario>[a-z0-9]+)",
    "(?P<climate_variable>[a-z]+)",
    "(?P<region>(global))",
    "(?P<timestep>[a-z0-9-]+)",
    "(?P<start_year>\\d{4})",
    "(?P<end_year>\\d{4})"
  ],
  "suffix": [
    ".nc"
  ]
}