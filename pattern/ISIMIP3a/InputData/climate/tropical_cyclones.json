{
  "path": [
    "(?P<simulation_round>[A-Za-z0-9]+)",
    "(?P<product>[A-Za-z]+)",
    "(?P<category>[a-z0-9-_]+)",
    "(?P<subcategory>[a-z0-9-_]+)",
    "(?P<climate_scenario>[a-z]+)",
    "(?P<region>(global|along-track))",
    "(historical)",
    "(?P<model>[A-Za-z0-9-.]+)"
  ],
  "dataset": [
    "(?P<model>[a-z0-9-.]+)",
    "(?P<climate_scenario>[a-z]+)",
    "(historical)",
    "(?P<climate_variable>[a-z]+)",
    "(?P<storm_id>[0-9NS]+)"
  ],
  "file": [
    "(?P<model>[a-z0-9-.]+)",
    "(?P<climate_scenario>[a-z]+)",
    "(historical)",
    "(?P<climate_variable>[a-z]+)",
    "(?P<storm_id>[0-9NS]+)"
  ],
  "suffix": [
    ".nc"
  ]
}
