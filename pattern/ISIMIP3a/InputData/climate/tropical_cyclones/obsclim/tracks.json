{
  "path": [
    "(?P<simulation_round>[A-Za-z0-9]+)",
    "(?P<product>[A-Za-z]+)",
    "(?P<category>[a-z0-9-_]+)",
    "(?P<subcategory>[a-z0-9-_]+)",
    "(?P<climate_scenario>[a-z]+)",
    "(tracks)",
    "(3hr)",
    "(historical)"
  ],
  "dataset": [
    "(tracks)",
    "(?P<climate_scenario>[a-z]+)",
    "(historical)"
  ],
  "file": [
    "(tracks)",
    "(?P<climate_scenario>[a-z]+)",
    "(historical)",
    "(?P<start_year>\\d{4})",
    "(?P<end_year>\\d{4})"
  ],
  "suffix": [
    ".nc"
  ]
}
