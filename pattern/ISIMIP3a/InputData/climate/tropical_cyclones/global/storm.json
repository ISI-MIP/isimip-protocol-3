{
  "path": [
    "(?P<simulation_round>[A-Za-z0-9]+)",
    "(?P<product>[A-Za-z]+)",
    "(?P<category>[a-z0-9-_]+)",
    "(?P<subcategory>[a-z0-9-_]+)",
    "(?P<region>(global))",
    "(storm)",
    "(historical)",
    "(?P<model>[A-Za-z0-9-.]+)"
  ],
  "dataset": [
    "(?P<model>[a-z0-9-.]+)",
    "(?P<climate_scenario>[a-z]+)",
    "(historical)",
    "(?P<climate_variable>[a-z]+)"
  ],
  "file": [
    "(?P<model>[a-z0-9-.]+)",
    "(?P<climate_scenario>[a-z]+)",
    "(historical)",
    "(?P<climate_variable>[a-z]+)",
    "(?P<start_year>\\d{4})"
  ],
  "suffix": [
    ".nc"
  ]
}
